// IMPORTANT: place a generated token from https://github.com/settings/tokens/new

export const token = ``

// when requesting notifications from REST - notifications permissions needed
// when searching with graphQL - token *is* required but doesn't need any permission)
